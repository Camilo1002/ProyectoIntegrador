<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= movie.title %> </title>
    <link rel='stylesheet' href='/css/style.css' />

</head>
<body>
    
    <h1><%= movie.title %> </h1>    
    <br>
    <p>Titulo: <%= movie.title %> </p>
    <p>RATING: <%= movie.rating %> </p>
    <p>AWARDS: <%= movie.awards %> </p>
    <p>LENGTH: <%= movie.length + ' min'%> </p>
    <p>RELEASE DATE: <%= movie.release_date %> </p>

    <% if(movie.genres){%> 
        <div>Genero: <%=movie.genres.name%></div>
        <%} %>
    <div>Actores: 
        <ul>
            <% for (let i=0; i<movie.actors.length; i++){ %>
                <li>
                    <%= movie.actors[i].first_name %> <%= movie.actors[i].last_name %>
                    
                </li>
                <% } %> 
         </ul>
    </div>
    <br>
    <p><a  class="botonVolver" href="/movies">Listado de Películas</a></p>

    <a href="/movies/edit/<%= movie.id %>"><button type="submit">Editar</button><br><br></a>
    <a href="/movies/delete/<%= movie.id %>"><button type="submit">Eliminar</button><br><br></a>
   

</body>
</html>